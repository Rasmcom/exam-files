# سياسة الأمان

## أسرار لا تُرفع إلى المستودع

- `SUPABASE_SERVICE_ROLE_KEY`
- ملف `.env.owner`
- كلمة مرور المالك

مفتاح `service_role` مخصص فقط لتشغيل سكربت إنشاء المالك من جهاز موثوق، ولا يجوز وضعه في متغير يبدأ بـ `VITE_` أو تضمينه في ملفات الواجهة.

## ضوابط الرفع

- الحد الأقصى للملف: 50 ميجابايت.
- الامتدادات المسموحة محددة داخل الواجهة وقاعدة البيانات وحاوية التخزين.
- الملفات التنفيذية والبرمجية غير مسموحة.
- حاوية التخزين خاصة، والتحميل يتم بروابط مؤقتة.

## إعدادات Supabase المطلوبة

- تعطيل تسجيل المستخدمين الجدد.
- عدم تفعيل روابط عامة لحاوية `exam-files`.
- تفعيل حماية كلمات المرور المسربة إن كانت متاحة في الخطة.
- استخدام كلمة مرور طويلة وفريدة للحساب الوحيد.
- تفعيل المصادقة متعددة العوامل لاحقًا عند اعتمادها في الواجهة.

## الإبلاغ عن مشكلة

لا تُرفق أسرارًا أو ملفات اختبارات حقيقية في بلاغات GitHub. اكتب خطوات إعادة المشكلة باستخدام بيانات تجريبية فقط.
